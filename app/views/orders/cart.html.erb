<% @order.line_items.each do |item| %>
  <%= item.quantity %> 
  <%= item.product.name %>
  <%= item.product.price %> 
<% end %>     
<%= button_to "go to checkout",checkout_orders_path(@order),method: :get,class:"btn btn-primary btn-block"%>
    